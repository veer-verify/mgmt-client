import { TextAndNumberOnlyDirective } from './text-and-number-only.directive';

describe('TextAndNumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new TextAndNumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
